version: '3.8'
services:
  postgresdb:
    image: postgres:17
    ports:
      - '5434:5432'
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: rukhaam2006
      POSTGRES_DB: formbuilder
      
    volumes:
      - 'postgres_data:/var/lib/postgresql/data'


volumes:
  postgres_data:
